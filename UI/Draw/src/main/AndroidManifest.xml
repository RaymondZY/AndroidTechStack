<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="zhaoyun.techstack.android.draw">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <meta-data
            android:name="design_width_in_dp"
            android:value="360"/>
        <meta-data
            android:name="design_height_in_dp"
            android:value="640"/>

        <activity android:name=".maskfilter.MaskFilterActivity"></activity>
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".style.StyleActivity" />
        <activity android:name=".shader.ShaderExampleActivity" />
        <activity android:name=".particle.ParticleEffectActivity" />
        <activity android:name=".colorfilter.PorterDuffColorFilterActivity" />
        <activity android:name=".transfermode.PorterDuffTransferModeActivity" />
        <activity android:name=".bezier.BezierActivity" />
        <activity android:name=".fontmetrics.FontMetricsActivity" />
        <activity android:name=".patheffect.PathEffectActivity" />
        <activity android:name=".shader.ShaderActivity" />
        <activity android:name=".colorfilter.LightingColorFilterActivity" />
    </application>

</manifest>