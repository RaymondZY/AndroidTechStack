apply from: rootProject.file('app.gradle')

android {
    defaultConfig {
        applicationId "zhaoyun.techstack.android.coordinatorlayout.basic"
    }
}

dependencies {
    implementation COORDINATOR_LAYOUT
}
