apply from: rootProject.file('app.gradle')

android {
    defaultConfig {
        applicationId "zhaoyun.techstack.android.service"
    }
}

dependencies {
    implementation LOCAL_BROADCAST_MANAGER
}
